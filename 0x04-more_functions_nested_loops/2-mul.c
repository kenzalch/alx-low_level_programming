#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 * @a: first one
 * @b: second one
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
